
span.sprite {
  display: inline-block;
}

{{#spritesheet}}
.sprite {
  background-image: url({{{escaped_image}}});
}
{{/spritesheet}}


{{#sprites}}
.sp-{{name}} {
  background-position: {{px.offset_x}} {{px.offset_y}};
  width: {{px.width}};
  height: {{px.height}};
}
{{/sprites}}